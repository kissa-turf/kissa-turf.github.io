# 喫茶ターフ 公開フォルダ — ホワイトリスト方式
# 原則すべてを無視し、公開してよいものだけを明示的に許可する。
# x_config.txt / *.csv / *.pkl / *.py が万一このフォルダに紛れても、絶対に push されない安全装置。
*
!.gitignore
!index.html

# 累積カーブを画像ファイルで参照している場合のみ、下を有効化する：
# !equity.png
